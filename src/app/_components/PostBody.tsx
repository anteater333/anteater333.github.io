"use client";

import { Category } from "@/interfaces/post";
import { PostAnalysis } from "@/lib/postAnalyzer";

const PostBody = function ({}: { content: string }) {
  return <></>;
};

export default PostBody;
